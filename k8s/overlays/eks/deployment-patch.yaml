apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-deployment
  namespace: shop
spec:
  template:
    spec:
      serviceAccountName: shop-serviceaccount
      imagePullSecrets:
        - name: ghcr-secret
